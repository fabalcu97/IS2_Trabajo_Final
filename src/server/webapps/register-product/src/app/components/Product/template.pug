html
  body.main
    .normal-screen
      .container.min-height(style='margin-top: 20px;')
        .row
          .col-xs-4.col-md-offset-8
            #card.login
              .front.signin_form
                p Register Product
                form.login-form
                  .form-group
                    .input-group
                      input.form-control(type='text', placeholder='Type the product name')
                      span.input-group-addon
                        i.glyphicon.glyphicon-user
                  .form-group
                    .input-group
                      input.form-control(type='number', placeholder='Type unit price')
                      span.input-group-addon
                        i.glyphicon.glyphicon-lock
                 
                  .form-group
                    .input-group
                      input.form-control(type='number', placeholder='Type unit weight')
                      span.input-group-addon
                        i.glyphicon.glyphicon-lock
                  .form-group
                    .input-group
                      input.form-control(type='text', placeholder='Type the unit of measurement')
                      span.input-group-addon
                        i.glyphicon.glyphicon-envelope
                  .form-group
                    .input-group
                      input.form-control(type='date', placeholder='Type the date')
                      span.input-group-addon
                        i.glyphicon.glyphicon-envelope
                  .form-group
                    .input-group
                      input.form-control(type='number', placeholder='Type quantity per lot')
                      span.input-group-addon
                        i.glyphicon.glyphicon-envelope
                  .form-group.sign-btn
                    input.btn(type='submit', value='Register')
                    br
    script(src='https://cdnjs.cloudflare.com/ajax/libs/jQuery-Flip/1.0.18/jquery.flip.js')
